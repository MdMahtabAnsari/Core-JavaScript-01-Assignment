/*
Q3. Use a nasted ternary check that a number is positive, negative or zero. You have to print "positive" if the number is positive and similarly for negative and zero also
*/
let num = 50
let output = null
output = (num>0) ? "positive" : (num<0) ? "negative" : "zero"
console.log(output);